# Read this aloud (for the bake-off)

Start speaking **immediately** when recording begins (don't leave dead air at the
front — it inflates the latency number), at a normal dictation pace.

---

> Okay let's set up the project. First clone the **git** repo from **GitHub**, then
> check out a new **branch** and run **npm** install. Open the **nginx** config and
> **grep** for the **localhost** port. We need to fix the **OAuth** flow in the
> **async** handler, add a **regex** to validate the **JSON** payload, and write a
> **SQL** query against the **Postgres** database. Then run **sudo** **kubectl** apply
> to deploy the **container**, **commit** the change, **rebase** onto main, and
> **push**. Finally restart the **Redis** cache and tail the logs over **SSH**.

---

The **bold** words are the 21 IT terms the bake-off scores for recognition.
